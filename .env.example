# PostgreSQL connection for the dashboard
# Copy this file to .env and set your actual values
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=change_me
DB_NAME=msc_proj
DATABASE_URL=postgresql://postgres:change_me@localhost:5432/msc_proj

# Dashboard data source (defaults to output/results if not set)
# CSV_DIR=output/results/final1200
